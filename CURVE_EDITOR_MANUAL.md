# 🧠 曲线编辑器开发文档 Curve Editor – 高级功能说明

## 📌 项目目标
提供一个网页界面，用户可以：
- 自定义坐标范围
- 鼠标点击添加点
- 自动连接曲线
- 支持 `.dat` 数据文件导入/导出
- 提供数据点移动、删除、批量编辑（复制、旋转）功能
- 使用键盘精确控制
- 实现所见即所得的曲线编辑与数据管理

> **v1.2 更新**：刷新页面时会清空所有临时数据，不再自动保存到 `localStorage`。

---

## 🔧 现有功能回顾

| 功能               | 描述                                 |
|------------------|------------------------------------|
| 坐标范围设置        | 用户输入 X/Y 最小最大值，重新设定绘图坐标轴          |
| 点选添加数据点       | 点击画布添加点，记录坐标点列表                      |
| 曲线绘制           | 使用平滑插值连接所有点                           |
| `.dat` 导出       | 下载当前数据点，格式为两列 `x\ty`                  |
| 清空画布          | 删除所有点与曲线                             |

---

## 🔼 新增功能列表（详细说明）

### 1. 📂 `.dat` 文件导入功能
#### ✅ 功能目标
上传已有 `.dat` 文件，自动识别数据点并在画布中渲染。

#### 🧩 实现细节
- 文件上传 `<input type="file" accept=".dat">`
- 解析文本行，跳过注释 `#`
- 格式要求：每行一个 `x\ty` 数据点
- 成功导入后：
  - 原图清空
  - 渲染点和对应曲线
  - 自动更新 X/Y 轴范围（或可选“保持原图比例”选项）

---

### 2. 🗑️ 数据点删除功能
#### ✅ 功能目标
鼠标点击某个点可将其删除，或通过键盘 `Delete` 删除选中点。

#### 🧩 实现方式
- 鼠标 hover 某点时高亮，并显示“删除”按钮或右键菜单
- 鼠标点击删除图标可移除该点
- 支持“框选 + 删除”多点删除

---

### 3. 📋 批量复制、旋转
#### ✅ 功能目标
将一组选中的点进行复制或旋转，快速构造周期性或对称图形。

#### 🧩 交互方式
- 框选一批点（Shift + 拖动矩形）
- 弹出操作栏：
  - `复制`: 将选中点向指定方向偏移生成副本
  - `旋转`: 以选中中心或原点为旋转中心，输入角度后旋转
- 所有复制/旋转操作均可撤销

---

### 4. ⌨️ 键盘方向键移动
#### ✅ 功能目标
选中一个或多个点后，按方向键进行细微位置调整

#### 🧩 控制方式
- `↑ ↓ ← →`：每次移动 0.01 单位（可调）
- `Shift + 方向键`：加倍精度（如 0.1 单位）
- 操作实时更新坐标、曲线

---

### 5. 🔁 撤销/重做
#### ✅ 功能目标
用户误操作后可以恢复之前的状态

#### 🧩 实现方式
- 使用操作栈 `undoStack[]`, `redoStack[]`
- 所有增删改动作都 push 到栈中
- 快捷键：`Ctrl+Z` 撤销，`Ctrl+Y` 重做

---

### 6. 💾 自动保存 + 快照功能
#### ✅ 功能目标
防止用户误刷新丢失数据

#### 🧩 功能说明
- 自动将当前点集保存到 `localStorage`
- 页面刷新自动恢复
- 可生成“快照文件”保存历史版本（带时间戳）

---

### 7. 🔍 数据表查看与编辑
#### ✅ 功能目标
以表格形式查看/编辑所有点

#### 🧩 表格功能
- 表格显示所有 `(x, y)`
- 可单独编辑某行数据（输入框）
- 可排序、批量复制、删除
- 可导出为 `.csv`、`.dat`

---

## 🔄 操作交互汇总（快捷键表）

| 操作                    | 快捷键或方式                |
|-----------------------|-------------------------|
| 添加点                  | 鼠标点击画布位置              |
| 删除点（单个）             | 点击点后点击删除按钮 / Delete 键 |
| 批量框选点                | Shift + 拖动鼠标               |
| 复制选中点                | Ctrl + C                       |
| 粘贴点                   | Ctrl + V                       |
| 移动选中点                | 方向键 ←↑→↓                  |
| 加精度移动               | Shift + 方向键                |
| 撤销                    | Ctrl + Z                  |
| 重做                    | Ctrl + Y                  |
| 导入 `.dat` 文件        | 选择上传按钮                  |
| 导出 `.dat` 文件        | 点击“导出 .dat”按钮           |

---

## 💡 高级功能建议（可选拓展）

| 功能             | 描述 |
|----------------|------|
| SVG 导出图像     | 导出为矢量图方便科研投稿或论文绘图 |
| 多轨迹管理       | 支持不同颜色多条曲线，每条独立操作 |
| 曲线插值方式切换   | 折线、样条、贝塞尔等多种可选方式 |
| 网格自定义       | 修改密度、样式、背景 |
| 激光场模型叠加模拟 | 可绘制额外的数学函数进行对比（如正弦波） |
| 坐标系标注功能    | 可手动添加箭头、文字标记等说明元素 |
| 自定义单位显示    | 支持物理单位如 fs、eV 显示 |

---

## 📁 文件格式说明

### `.dat` 示例格式：

x\ty

0.000000\t1.200000
0.500000\t1.500000
1.000000\t2.000000

---

## 🚀 技术栈建议

| 功能模块     | 推荐技术         |
|------------|----------------|
| 绘图        | D3.js、Canvas |
| 数据处理     | JavaScript (ES6+) |
| 文件导出导入 | Blob, FileReader |
| 框选 & 拖动  | D3 拖拽行为 / SVG 自定义框选 |
| 状态管理     | JS 数据结构：数组 + undo 栈 |

---

## ✅ 开发建议
- 按功能模块组织 JS 文件，如 `draw.js`、`edit.js`、`file.js`、`keyboard.js`
- 所有点数据集中存在 `points[]`
- 所有状态变化都应同步：SVG + 数据结构 + 文件导出

