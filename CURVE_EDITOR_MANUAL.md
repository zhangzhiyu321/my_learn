# Curve Editor Manual

## 基本功能
- **坐标范围设置**：填写 X/Y 最小值和最大值并点击 *Apply Range* 以重绘坐标轴。
- **添加点**：在绘图区单击即可创建数据点，曲线会自动连接所有点。
- **拖动与框选**：直接拖动点可移动位置，按住 Shift 并拖曳可框选一批点。
- **取消选择**：点击空白区域会取消当前选中的点。
- **删除**：选中点后按 Delete 或 Backspace 键删除。
- **键盘微调**：方向键可微调选中点，按住 Shift 则步长加倍。
- **导入/导出**：通过侧栏按钮载入或保存 `.dat` 文件。
- **复制/粘贴**：使用快捷键 Ctrl/Cmd+C、Ctrl/Cmd+V。
- **旋转**：输入角度后点击 *Rotate*，将所选点围绕质心旋转。
- **片段采样**：输入采样数量并点击 *Sample Segment* 生成均匀点。
- **撤销/重做**：Ctrl+Z/Ctrl+Y 或点击对应按钮。
- **曲线显示开关**：点击 *Hide Line* 按钮切换曲线可见性。
- **背景图片**：通过 *Set Background* 上传图片作为坐标系背景，可辅助拟合曲线；点击 *Clear Background* 移除。
- **清空画布**：点击 *Clear* 可删除所有点和曲线。

编辑过程会自动保存到浏览器的 localStorage，刷新后会自动恢复。
